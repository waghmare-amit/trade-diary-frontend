import { Container, Typography } from "@mui/material";

export default function Settings() {
  return (
    <Container>
      <Typography variant="h4" mt={4}>Settings & Profile</Typography>
      {/* Add dark/light toggle, profile info, etc */}
    </Container>
  );
}
